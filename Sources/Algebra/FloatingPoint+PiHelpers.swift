import Foundation


extension Double {
    static let twoPi = pi * 2.0
    static let halfPi = pi / 2.0
    static let quarterPi = pi / 4.0
}


extension Float {
    static let twoPi = pi * 2.0
    static let halfPi = pi / 2.0
    static let quarterPi = pi / 4.0
}
